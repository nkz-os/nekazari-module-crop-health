# =============================================================================
# nekazari-module-crop-health — Python dependencies
# =============================================================================

# Core framework
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0

# Async HTTP client
httpx>=0.27.0,<1.0

# Redis async
redis>=5.0.0,<6.0

# JWT authentication
PyJWT[crypto]>=2.8.0,<3.0

# Caching
cachetools>=5.3.0,<6.0

# Database (weather data from TimescaleDB)
asyncpg>=0.29.0,<1.0

# Dev / Test
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
# nkz-platform-sdk — uncomment when published to PyPI or use git dependency:
# nkz-platform-sdk @ git+https://github.com/nkz-os/nkz.git#subdirectory=services/nkz-platform-sdk
